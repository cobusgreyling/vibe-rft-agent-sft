# Vibe RFT — SFT on agent traces
# Free Colab T4: QLoRA (bitsandbytes) + LoRA (peft) + TRL SFTTrainer
#
#   pip install -r requirements.txt

torch>=2.2.0
transformers>=4.46.0
datasets>=3.0.0
trl>=0.14.0
peft>=0.13.0
accelerate>=1.0.0
bitsandbytes>=0.44.0
sentencepiece>=0.2.0
protobuf>=4.25.0
tqdm>=4.66.0
numpy>=1.26.0
